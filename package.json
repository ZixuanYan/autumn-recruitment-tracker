{
  "name": "autumn-mail-sync",
  "version": "0.3.0",
  "private": true,
  "description": "定时/手动 GitHub Action：IMAP 增量拉取 QQ 邮箱招聘邮件 → 关键词预筛 → AI 判定归属+阶段+细节 → 只写用户私有 Gist 的 mail-suggestions.json（网页端 autumn-recruitment-tracker 人工复核后并入台账）。密钥只在本私有仓库 Secrets，永不进浏览器/vault。",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "main": "index.js",
  "scripts": {
    "connectivity": "node src/connectivity-test.js",
    "sync": "node index.js",
    "report-error": "node index.js --report-error",
    "check": "node --check index.js && node --check src/config.js && node --check src/crypto.js && node --check src/imap.js && node --check src/prefilter.js && node --check src/parse.js && node --check src/state.js && node --check src/gist.js && node --check src/ai.js && node --check src/connectivity-test.js",
    "test": "node test/run.js && node test/integration.js && node test/web-check.js && node test/web-runtime.js"
  },
  "dependencies": {
    "imapflow": "^1.0.164",
    "mailparser": "^3.7.1"
  }
}
